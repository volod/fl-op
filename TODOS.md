# fl-op Implementation TODOs

Generated by /plan-eng-review on 2026-05-21. Build order matches design doc Next Steps.

## Phase 1: Foundation

- [ ] T01 Init pyproject.toml with uv; add ortools, numpy, scipy, pydantic>=2, click
- [ ] T02 Create src/fl_op/__init__.py and src/fl_op/main.py CLI skeleton (click group,
       4 subcommands: generate-data, solve, reschedule, query-contract)
- [ ] T03 Write src/fl_op/constants.py: power_margin_pct threshold, weather restriction
       limits (wind_ms, rain_mm, soil_moisture_pct), default cluster solve time limit,
       fuel/fertilizer cost rates, scoring weights, cluster target size, haversine
       earth radius in km
- [ ] T04 Write src/fl_op/models/enums.py: OperationType (SPRAYING TILLAGE SEEDING
       HARVESTING FERTILIZING), ImplementType, VehicleType as Python Enums

## Phase 2: Data Model

- [ ] T05 Write src/fl_op/models/types.py: TypedDict definitions for ClusterSpec,
       FeasibleAssignment, DispatchPackage, InfeasibleOrder; used by all solver
       modules; validated at Pool.submit and after worker return
- [ ] T06 Write src/fl_op/models/vehicle.py (Pydantic v2 BaseModel)
- [ ] T07 Write src/fl_op/models/implement.py (Pydantic v2; ImplementType enum field)
- [ ] T08 Write src/fl_op/models/operator.py (Pydantic v2; shift_start/shift_end)
- [ ] T09 Write src/fl_op/models/depot.py (Pydantic v2; lat/lon location)
- [ ] T10 Write src/fl_op/models/field.py and order.py (OperationType enum field)
- [ ] T11 Write src/fl_op/models/contract.py and weather.py; add @field_validator
       bounds (area_ha max 50000, polygon vertex count max 1000, datetime range max 5yr)
- [ ] T12 Write src/fl_op/models/compat_matrix.py: build_compat_matrix() returning
       (ndarray bool shape N_v x N_i, ndarray float32 power_margin same shape);
       serialize as numpy.memmap .npy pair; workers load via np.load(mmap_mode='r')
       for zero-copy access (avoids 100MB pickle per worker call)
- [ ] T13 Wire model_rebuild() in src/fl_op/models/__init__.py for circular refs
       (Vehicle<->Operator, Order<->Contract); use TYPE_CHECKING for forward refs

## Phase 3: Synthetic Data Generator

- [ ] T14 Write src/fl_op/data/generator.py: NumPy vectorized sampling; scipy BallTree
       haversine for depot/field geographic placement; realistic distributions
       (power curves lognormal, seasonal demand, contract price/penalty);
       generate all entity types; write CSV + JSON to .data/generate-data/<ts>/;
       support --data-path DIR for real CSV import (missing fields default to synthetic)
- [ ] T15 Wire generate-data CLI command; validate output schema via Pydantic load;
       add path validation: resolve all input paths with pathlib.Path.resolve(),
       raise click.BadParameter on traversal outside allowed roots

## Phase 4: Pre-Filter + Clustering + Pre-Allocation

- [ ] T16 Write src/fl_op/solver/preprocessing.py: numpy compat matrix lookup for
       power filter (vectorized); OperationType enum filter; haversine BallTree
       depot-affinity cluster_orders_by_depot(); returns TypedDict ClusterSpec list
- [ ] T17 Write src/fl_op/solver/resource_allocator.py: allocate_resources(clusters)
       sorts clusters by sum(order.penalty_per_day) descending (penalty-weighted
       urgency, NOT raw order count); reserves implement_id and operator_id to first
       claiming cluster; collapses each physical vehicle_id to one V-I pair per cluster
       (highest greedy score wins); tiebreak by cluster_id for determinism

## Phase 5: Greedy Warm-Start

- [ ] T18 Write src/fl_op/solver/greedy.py: vectorized_score() numpy broadcast over
       all feasible V-I pairs x orders (5M+ evaluations; no Python-level loops);
       score = gross_margin_estimate - repositioning_cost; greedy_assign() top-1
       per order; return warm-start dict for OR-Tools initial solution hint

## Phase 6: Cluster Solver

- [ ] T19 Write src/fl_op/solver/cluster_solver.py: solve_cluster(cluster_dict) ->
       tuple[list[dict], list[dict]]; OR-Tools routing library model (not CP-SAT);
       time window dimensions from contract deadlines + operator shifts (hard);
       resource-uniqueness via AllowedVehicles or vehicle incompatibility lists;
       inventory arc costs (fuel_L, fertilizer_kg dimensions); depot restocking nodes;
       num_search_workers=1; routing model created and destroyed inside function;
       all inputs/outputs plain Python dicts; return (dispatch_packages, infeasible_orders)
       even when no solution found (never raise on solver timeout)

## Phase 7: Aggregation + Solve Command

- [ ] T20 Write src/fl_op/solver/aggregator.py: pool_solve(clusters) via
       multiprocessing.Pool(start_method="spawn", maxtasksperchild=1);
       per-task timeout via AsyncResult.get(timeout=); on WorkerLostError or
       TimeoutError mark cluster orders infeasible("solver_timeout");
       assert len(result)==2 per worker; merge; compute KPIs including
       greedy_baseline_margin_EUR; write schedule_kpis.json alongside
       schedule_report.txt; add schema_version and run_metadata to all JSON output
- [ ] T21 Wire solve CLI command: load data -> preprocessing -> pre-allocation ->
       greedy -> aggregator -> write .data/solve/<ts>/schedule.json,
       schedule_report.txt, schedule_kpis.json, infeasible_orders.json;
       on exit 1 (all infeasible) print "0 served / N rejected" with top 3
       infeasibility reasons and suggested fix to stdout;
       support --latest for --data and --schedule flags

## Phase 8: Reschedule Command

- [ ] T22 Write src/fl_op/solver/reschedule.py: load schedule.json; validate
       OrderStatus enum (raise on unknown, never default to pending); freeze
       orders with status "started"; guard: if remaining_orders empty, write
       empty schedule.json + plan_diff.json("no_unstarted_orders") and exit 0;
       re-run pre-allocation + solver on remaining; generate plan_diff.json
       (structured) and plan_diff.txt (human summary from plan_diff.json)
- [ ] T23 Wire reschedule CLI command; write .data/reschedule/<ts>/;
       support --schedule latest; support mark_started event type

## Phase 9: Query-Contract Command

- [ ] T24 Write src/fl_op/solver/query.py: run preprocessing pre-filter on new
       order only; greedy_score all feasible pairs; build in-memory
       dict[vehicle_id, list[TimeWindow]] index from schedule.json on load;
       compute schedule_conflict_risk via index lookup (not linear scan);
       return top-3 sorted by estimated_margin_EUR desc with stable tiebreak
       by vehicle_id; no OR-Tools solver call
- [ ] T25 Wire query-contract CLI command; write .data/query-contract/<ts>/;
       support --data latest and --schedule latest

## Phase 10: DX Infrastructure

- [ ] T26 Add Makefile with: venv (uv sync), setenv (.env from .env.example),
       quickstart (generate-data + solve at smoke-test scale: 50 vehicles / 200
       implements / 20 orders / 5 depots), data (full benchmark, documented as manual)
- [ ] T27 Write README.md Quick Start section: copy-paste smoke-test commands,
       example output, link to full design doc; label default benchmark commands as
       "manual benchmark" not CI target
- [ ] T28 Add --latest resolver to all commands that accept --data or --schedule;
       resolves to newest timestamp directory under .data/<command>/

## Phase 11: Tests

- [ ] T29 Write tests/conftest.py: small synthetic dataset fixture
       (--vehicles 50 --implements 200 --orders 20 --depots 5)
- [ ] T26 Write tests/test_models.py: T01-T05 (enum mismatch, matrix shape,
       model_rebuild, JSON round-trip)
- [ ] T27 Write tests/test_preprocessing.py: T06-T10 (power filter edge cases,
       haversine depot affinity, cluster membership)
- [ ] T28 Write tests/test_resource_allocator.py: T11-T13 (no cross-cluster
       duplicate, larger cluster wins, all-infeasible cluster)
- [ ] T29 Write tests/test_greedy.py: T14-T15 (vectorized, correct assignment)
- [ ] T30 Write tests/test_cluster_solver.py: T16-T23 (worker I/O types, tuple
       return, infeasible not dropped, num_search_workers, resource-uniqueness,
       time window hard constraints)
- [ ] T31 Write tests/test_aggregator.py: T24-T27 (infeasible collection, no
       cross-cluster overlap, KPI sum, exit codes)
- [ ] T32 Write tests/test_reschedule.py: T28-T29 (frozen orders, plan_diff fields)
- [ ] T33 Write tests/test_query.py: T30-T31 (no solver call, deterministic top-3)
- [ ] T34 Write tests/test_smoke.py: E2E generate-data -> solve -> reschedule ->
       query-contract on small fixture; assert no crash and infeasible_orders.json written
